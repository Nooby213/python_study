# function_vs_method.py (특히, str.method)
# 헷갈리기 시작하는 것
# 언제 어느 순간의 어떤 함수는 이렇게 작동하늗네
# 얘는 왜 이렇게 작동함?
# 이거 외워야 함?
# 외우다 > 이해의 범주

# list의 메서드는 대체적으로 원본을 바꾸는게 하는 일임
# list.append

# 파이썬은 함수의 return 값이 반드시 존재한다. (return을 정의안하면 None)
# 함수는 반드시 어떤 값을 반환해야 한다. X

# 근데 문자열 메서드는 죄다 반환값이 있는것 같음
# 문자열은 원본 수정이 안되기 때문에